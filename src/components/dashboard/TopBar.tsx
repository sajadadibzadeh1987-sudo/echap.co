// src/components/dashboard/TopBar.tsx
"use client";

/**
 * TopBar کاملاً حذف شده تا فقط SiteHeader جدید نمایش داده شود.
 * این کامپوننت فقط برای سازگاری با DashboardLayout باقی مانده است.
 */
const TopBar = () => {
  return null;
};

export default TopBar;
